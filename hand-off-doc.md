# hand-off-doc.md

## 未完成事項

### Email 與全局設定改造

優先度：低於「統一打包與桌面啟動改造」。本項需在前一項完成或至少不衝突後再實作，尤其要配合 SQLite 與桌面版資料保存方式。

#### 已確認需求

- 週檢視表 Email 的備注文字要放大。
- 週檢視表 Email 的最後發貨日要使用更明顯的顏色，例如紅色。
- 新增月檢視表 Email 發送功能，內容以清楚呈現排程時間為主。
- 週檢視表 Email 若輸出範圍超過一週，不可一直往右增加天數；需要換行或拆成多段，讓內容好閱讀、好列印。
- 新增完工統計表 Email 發送功能，可選擇時間範圍。
- 完工統計表刪除「買家暱稱」欄位。
- Email 發送功能移到全局入口；點擊後可選擇要發送週表、月表或完工統計表，並選擇時間範圍與收件者。
- 新增全局設定，可設定預估工時的基礎金額；目前固定 `100` 的設計需改為使用者可調整。
- 「排程 Email 已送出」提示只顯示 5 秒，之後自動消失。

#### 假設與不確定性

- 月檢視表 Email 不需要完全複製前端月曆視覺，重點是清楚列出日期、訂單、開始/結束時間、工時與必要備注。
- 完工統計表 Email 應沿用現有完工統計資料邏輯，只增加 Email 輸出與日期範圍篩選，不另外建立一套統計口徑。
- 全局 Email 入口可放在目前主畫面頂部導覽或其他全局工具區，但要避免讓日曆工具列承擔所有 Email 類型。
- 預估工時基礎金額需要持久化；若前一項已改為 SQLite，應存在 SQLite 中，不應只放 localStorage。
- 「買家暱稱」欄位刪除需同步處理前端表格、Email、DTO 或後端查詢，避免仍有無用欄位留在資料流中。

#### 可驗證完成條件

1. 使用者可從全局 Email 入口開啟發送對話框，選擇週表、月表或完工統計表。
2. 全局 Email 對話框可輸入收件者、主旨與時間範圍，並依選擇的 Email 類型送出正確內容。
3. 週檢視表 Email 中備注文字比現況更易讀，最後發貨日有明顯顏色標示。
4. 週檢視表 Email 在日期範圍超過一週時會分行或分段呈現，不會無限向右延展。
5. 月檢視表 Email 能清楚呈現每筆排程的日期、時間區間與訂單資訊。
6. 完工統計表 Email 可依使用者選擇的時間範圍發送，且內容不包含「買家暱稱」欄位。
7. 前端完工統計表不再顯示「買家暱稱」欄位。
8. 使用者可在全局設定調整預估工時基礎金額，儲存後會影響完工統計表中的時薪或相關計算。
9. 「排程 Email 已送出」提示會在 5 秒後自動消失，不需要使用者手動清除。
10. 完成後更新根目錄 `README.md` 的 Email 類型、全局設定與完工統計說明，並從本文件移除本交接事項。

#### 建議實作方向

- 先盤點現有 `ScheduleEmailDialog.vue`、`WorkOrderCalendar.vue`、`CompletedStatsTable.vue`、`workOrderStore.js`、`WorkOrderEmailService`、`WorkOrderController` 與 Email template，再決定全局 Email 對話框與 API payload 結構。
- 可將 Email 類型抽成明確 enum / 常數，例如 `WEEK`、`MONTH`、`COMPLETED_STATS`，但只在能減少分支混亂時才新增抽象。
- 週表、月表與完工統計表可以共用寄信入口，但 Email 內容產生應保持各自清楚，避免在同一個 template 裡堆疊過多條件。
- 週表超過一週的換行規則需先定義，例如每 7 天一段，或依自然週切段；以列印穩定為優先。
- 全局設定先以最小可用資料模型保存基礎金額即可，不需要提前做大型設定中心。
- 提示 5 秒消失可在 store 或全局通知狀態中處理，避免每個元件各自 setTimeout。

#### 建議驗證

- 前端：檢查全局 Email 入口、類型切換、日期範圍、收件者輸入、成功提示 5 秒消失。
- 後端：執行 Email service 相關測試，新增週表跨週、月表、完工統計表與基礎金額設定測試。
- 視覺：用實際產生的 Email HTML 檢查週表備注字級、最後發貨日顏色、跨週換行與列印版面。
- 統計：確認刪除買家暱稱後，前端表格、Email、API response 與測試都不再依賴該欄位。
- 設定：重啟應用後確認預估工時基礎金額仍保留，且完工統計計算使用新設定值。
