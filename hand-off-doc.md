# hand-off-doc.md

## 未完成事項

### 完工統計欄位與全局設定改造

優先度：低於已完成的週表 / 月表 / 完工統計表 Email 調整。本項需配合 SQLite 與桌面版資料保存方式。

#### 已確認需求

- 完工統計表刪除「買家暱稱」欄位。
- 目前全局 Email 入口已支援週表、月表與完工統計表。
- 新增全局設定，可設定預估工時的基礎金額；目前固定 `100` 的設計需改為使用者可調整。
- 「排程 Email 已送出」提示只顯示 5 秒，之後自動消失。

#### 假設與不確定性

- 預估工時基礎金額需要持久化；若前一項已改為 SQLite，應存在 SQLite 中，不應只放 localStorage。
- 「買家暱稱」欄位刪除需同步處理前端表格、Email、DTO 或後端查詢，避免仍有無用欄位留在資料流中。

#### 可驗證完成條件

1. 前端完工統計表不再顯示「買家暱稱」欄位。
2. 完工統計表 Email 同步不再顯示「買家暱稱」欄位。
3. 使用者可在全局設定調整預估工時的基礎金額，儲存後會影響完工統計表中的時薪或相關計算。
4. 「排程 Email 已送出」提示會在 5 秒後自動消失，不需要使用者手動清除。
5. 完成後更新根目錄 `README.md` 的完工統計欄位、全局設定與提示行為說明，並從本文件移除本交接事項。

#### 建議實作方向

- 刪除「買家暱稱」時需同步處理 `CompletedStatsTable.vue`、`CompletedWorkOrderStatsResponse`、`WorkOrderEmailService` 與完工統計 Email template。
- 全局設定先以最小可用資料模型保存基礎金額即可，不需要提前做大型設定中心。
- 提示 5 秒消失可在 store 或全局通知狀態中處理，避免每個元件各自 setTimeout。

#### 建議驗證

- 前端：檢查完工統計表欄位、全局 Email 入口與成功提示 5 秒消失。
- 後端：執行 Email service 相關測試，新增基礎金額設定測試。
- 統計：確認刪除買家暱稱後，前端表格、Email、API response 與測試都不再依賴該欄位。
- 設定：重啟應用後確認預估工時基礎金額仍保留，且完工統計計算使用新設定值。
