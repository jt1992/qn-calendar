spring:
  datasource:
    url: jdbc:sqlite:target/qn-calendar-test.db
    driver-class-name: org.sqlite.JDBC
    username: ""
    password: ""
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  mail:
    host: localhost
    port: 1025

app:
  desktop:
    enabled: false
